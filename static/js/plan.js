$(document).ready(function(){
	$('#story').typed({
		strings: ["Hello."],
		backDelay: 500,
	        typeSpeed: 30,
                backSpeed: 30,
                startDelay: 1500,
                loop: false,
                loopCount: false,
                showCursor: false
	});
});
